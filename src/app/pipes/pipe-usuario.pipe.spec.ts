import { PipeUsuarioPipe } from './pipe-usuario.pipe';

describe('PipeUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
